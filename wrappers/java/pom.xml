<?xml version="1.0" encoding="UTF-8"?>
<!-- Minimal POM for consumers who build with Maven. The sandbox test suite
     does not use Maven; run ./run-tests.sh instead.

     On JDK 21 the java.lang.foreign FFM API is a preview feature, so both
     compilation and execution need \-\-enable-preview. On JDK 22+ the API is
     final and the flag can be dropped (set maven.compiler.release to 22 and
     remove the compilerArgs / argLine entries). -->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.hymma</groupId>
    <artifactId>hymma-licensing</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>hymma-licensing</name>
    <description>Java wrapper (java.lang.foreign) for the hymma license-management-core native library</description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>21</maven.compiler.release>
    </properties>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>21</release>
                    <compilerArgs>
                        <arg>--enable-preview</arg>
                    </compilerArgs>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.2.5</version>
                <configuration>
                    <argLine>--enable-preview</argLine>
                    <environmentVariables>
                        <HLM_TIMESYNC>off</HLM_TIMESYNC>
                    </environmentVariables>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
